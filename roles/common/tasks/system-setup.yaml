  - name: copy {{ component }} config
    ansible.builtin.template:
      src: {{ component }}.service
      dest: /etc/systemd/system/{{ component }}.service
      notifier: daemon reload

  - name: start service
    ansible.builtin.service:
      name: "{{ component }}"
      state: restarted
      enabled: yes